spring.application.name=springboot-oceanica

# Usamos las variables de entorno para la conexión
spring.datasource.url=jdbc:postgresql://localhost:5434/oceanicadb
spring.datasource.username=paula
spring.datasource.password=123

# Dialecto de Hibernate para PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Crear o actualizar tablas automáticamente
spring.jpa.hibernate.ddl-auto=update

#imagenes
image.upload.dir=uploads/images/

server.port=8081